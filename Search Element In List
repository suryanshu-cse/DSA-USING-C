#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>
int n;
int found=0;

void search(int list[],int value){
    for(int i=0;i<n;i++){
      if(list[i]==value){
          found=1;
          break;
      }
        }
    }

int main() {
scanf("%d",&n);
    int list[n];
    for(int i=0;i<n;i++){
        scanf("%d",&list[i]);
    }
    int value;
    scanf("%d",&value);
    search(list,value);
    if(found==1){
        printf("Found");
    }
    else{
        printf("Not Found");
    }
    
    
 
    return 0;
}
