#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {

    /* Enter your code here. Read input from STDIN. Print output to STDOUT */ 
    int n;
    scanf("%d",&n);
    int list[n];
    for(int i=0;i<n;++i){
        scanf("%d",&list[i]);
    }
   
        for(int i=0;i<n;++i){
            int count =0;
            for(int j=0;j<n;++j){
                if(list[j]==list[i])
                    count++;
            }
        if(count==1)
       
        printf("%d ",list[i]);
        }
    return 0;
}
